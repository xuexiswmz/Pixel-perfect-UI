interface:
  display_name: "Pixel-perfect-UI"
  short_description: "Reconstruct frontend UI with measured screenshot fidelity."
  default_prompt: "Use $pixel-perfect-ui to reconstruct this visual reference as working frontend code, then capture, diff, recover, and iterate until the measured result converges."

policy:
  allow_implicit_invocation: true
