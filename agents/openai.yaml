interface:
  display_name: "Frontend UI Builder"
  short_description: "Generate or patch presentation-layer UI code, including screenshot-guided local edits."
  default_prompt: "Use $frontend-ui-builder to generate or patch presentation-layer frontend UI code from a brief, UI-focused PRD, mockup, or screenshot with precise placement in the existing project."

policy:
  allow_implicit_invocation: true
