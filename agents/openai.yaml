interface:
  display_name: "Frontend UI Builder"
  short_description: "Build and verify faithful frontend UI from specs or screenshots."
  default_prompt: "Use $frontend-ui-builder to implement this frontend UI; for screenshots, recover missing measurements, generate code, render, diff, and iteratively correct it instead of stopping at validation failures."

policy:
  allow_implicit_invocation: true
